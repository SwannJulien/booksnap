meta {
  name: DeweyController
  seq: 4
}

auth {
  mode: inherit
}

docs {
  Reference data — all three reads are open to any signed-in user, students included.
  A session is still required: since US-009 nothing answers without one.
}
